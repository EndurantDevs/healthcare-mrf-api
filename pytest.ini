[pytest]
asyncio_mode = auto
addopts = --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    filterwarnings(warning): declared so collection survives -p no:warnings runs
testpaths = tests
